version: '3.4'
services:
  bpro:
    build:
      context: .devcontainer
      dockerfile: Dockerfile
      network: host
      args:
        USERNAME: ${USER}
        USERID: ${USERID}
        GROUPID: ${GROUPID}
        WEBPRO: ${WEBPRO}
        INTERNPRO: ${INTERNPRO}
        PLUGINSDK: ${PLUGINSDK}
        CRTOOL: ${CRTOOL}
        CRWRAP: ${CRWRAP}
    hostname: ${HNAME}
    volumes:
      - .:/bpvol
    stdin_open: true
    tty: true
# docker-compose build
# docker-compose run --rm bpro
