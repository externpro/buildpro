name: Build rocky-mdv image

on:
  push:
    branches: [ "dev" ]
    paths:
      - 'public/rocky-mdv.dockerfile'
  pull_request:
    branches: [ "dev" ]
    paths:
      - 'public/rocky-mdv.dockerfile'
  workflow_dispatch:
  workflow_run:
    workflows: ["rocky-pro"]
    types:
      - completed

jobs:
  rocky-mdv:
    uses: externpro/actpro/.github/workflows/buildpro.yml@main
    with:
      img: rocky-mdv
