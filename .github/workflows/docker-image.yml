name: Docker Image CI

on:
  push:
    branches: [ "dev" ]
  pull_request:
    branches: [ "dev" ]
  workflow_dispatch:

jobs:
  ubuntu:
    uses: externpro/buildpro/.github/workflows/jobs.yml@dev
    with:
      img: ubuntu
